{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install",
    "nixpacks": {
      "providers": ["nodejs"]
    }
  },
  "deploy": {
    "startCommand": "cd server && npm run dev"
  }
}
