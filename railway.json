{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd server && npm install",
    "railpack": {
      "providers": ["nodejs"]
    }
  },
  "deploy": {
    "startCommand": "cd server && npm run dev"
  }
}
